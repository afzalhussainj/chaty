# Public URL of the Next.js app (used for metadata and absolute links)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# FastAPI backend base URL (browser and server-side calls in later phases)
NEXT_PUBLIC_API_URL=http://localhost:8000
